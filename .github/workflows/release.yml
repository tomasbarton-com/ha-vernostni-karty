name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version (e.g. v0.3.1)"
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Resolve version
        id: ver
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Create tag (workflow_dispatch only)
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
          git tag ${{ steps.ver.outputs.tag }} || true
          git push origin ${{ steps.ver.outputs.tag }} || true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: ${{ steps.ver.outputs.tag }}
          body: |
            ## Věrnostní karty ${{ steps.ver.outputs.tag }}

            ### Instalace integrace přes HACS

            1. HACS → Integrace → tři tečky → Vlastní repozitáře
            2. Přidejte `https://github.com/tomasbarton-com/ha-vernostni-karty` jako typ **Integrace**
            3. Nainstalujte „Věrnostní karty" a restartujte HA

            ### Ovládací panel (Lovelace karta)

            Karta je v samostatném repozitáři `ha-vernostni-karty-card` – přidejte jej přes HACS jako **Frontend** plugin.
          files: |
            icon.png
          make_latest: true
